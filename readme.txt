Einmal initial mit "npm i" alle dependencies installieren.
Dann kann mit "node server/index.js" der Server gestartet werden, der auf localhost:50051 läuft.
Mit "node client/index.js" können dann die geforderten Anfragen getestet werden.